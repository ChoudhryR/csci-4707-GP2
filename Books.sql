CREATE TABLE Books (
    BookID INT PRIMARY KEY,
    Title VARCHAR(100),
    Genre VARCHAR(50),
    PublicationYear INT,
    Price DECIMAL(10,2),
    PublisherName VARCHAR(100),
    PublisherContactInfo VARCHAR(100)
);
